- name: command vs shell
  hosts: local
  connection: local
  tasks:
  - name: print the command
    ansible.builtin.command: ls -ltr > /tmp/output.txt

  - name: check the processe
    ansible.builtin.command: ps -ef | grep ssh
    register: output